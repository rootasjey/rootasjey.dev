import 'package:flutter/material.dart';

class DashboardWelcomePage extends StatelessWidget {
  const DashboardWelcomePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Dashboard Welcome"),
    );
  }
}
