enum AniProps {
  color,
  height,
  opacity,
  translateX,
  translateY,
  width,
}

enum DiscoverType {
  authors,
  references,
}

enum FeaturesLayout {
  table,
  wrap,
}

enum HeaderViewType {
  options,
  search,
}

enum ItemsLayout {
  list,
  grid,
}

enum ItemsStyle {
  list,
  grid,
}
